# Reporting User Guide v1

Open any workspace and use the Reports action row to export HTML, CSV or JSON. HTML opens in a browser tab. CSV downloads a simple flattened report. JSON exposes the source-backed report payload.
