"""
config — JOM configuration package

This package centralises feature flags and configuration loading
for the Jira Observation Monitor.

Phase 1: only feature_flags is used (and only with default values).
Phase 2: this package will expand to include runtime config loaders.
"""