from pathlib import Path
import re,sys
R=Path(__file__).resolve().parents[1];h=(R/'templates/governance_report.html').read_text(encoding='utf-8-sig');j=(R/'static/js/jom_governance_report_v1.js').read_text(encoding='utf-8-sig');c=(R/'static/css/jom_governance_report_v1.css').read_text(encoding='utf-8-sig')
checks={'phase owner':'JOM_GOVERNANCE_REPORT_UX_PHASE1_V1' in h and c.startswith('/* JOM_GOVERNANCE_REPORT_UX_PHASE1_V1 */'),'three existing project APIs':set(re.findall(r'"(/api/[^" ]+)"',j))=={'/api/governance/projects','/api/governance/projects/leads','/api/governance/projects/owners'},'no legacy report API':'/api/reporting/governance-report' not in j,'authority reconciliation':all(x in j for x in ['sets.some','Project authorities are not synchronized','new Map(leads.map','new Map(owners.map']),'governance measures':all(x in h for x in ['gr-projects','gr-owned','gr-coverage','gr-gaps','gr-distinct','gr-sites']),'gap workflow':'gr-gap-list' in h and '/reports/governance/projects#gp-gaps' in h and j.count('/reports/governance/projects#gp-gaps')>=1,'site coverage':'gr-site-grid' in h and 'bySite' in j,'owner concentration':'gr-owner-grid' in h and 'byOwner' in j,'authority health':'gr-health-grid' in h and 'contractState' in j,'unavailable domains':all(x in h for x in ['Users','Permissions','Configuration','Policy compliance','unavailable']),'contained fixed rail':all(x in c for x in ['position:fixed','max-height:calc(100vh - var(--gr-top) - var(--gr-bottom))','overflow-y:auto','scrollbar-gutter:stable']),'read only':'Read-only' in h,'no fabricated collectors':all(x not in j for x in ['POST','PUT','PATCH','DELETE','setInterval','WebSocket','EventSource']),'single terminal newline':all(p.read_bytes().endswith(b'\n') and not p.read_bytes().endswith(b'\n\n') for p in [R/'templates/governance_report.html',R/'static/js/jom_governance_report_v1.js',R/'static/css/jom_governance_report_v1.css'])}
f=[]
for k,v in checks.items():print(('PASS' if v else 'FAIL')+': '+k);f.append(k) if not v else None
if f:print('VALIDATION FAILED: '+str(len(f)));sys.exit(1)
print('VALIDATION PASS: Governance Report UX Phase 1 reuses synchronized Project Inventory, Project Lead and Space Owner authority without new backend or collector claims.')
