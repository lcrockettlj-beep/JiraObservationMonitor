# JOM legacy scheduler command retired
param()
$ErrorActionPreference = "Stop"
Write-Host "[JOM] Legacy scheduler commands are retired. Use the current runtime refresh and OAuth-backed runtime flow instead."
exit 0
