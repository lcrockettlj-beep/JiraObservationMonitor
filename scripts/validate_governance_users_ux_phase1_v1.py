from pathlib import Path
import re,sys
R=Path(__file__).resolve().parents[1];h=(R/'templates/governance_users.html').read_text(encoding='utf-8-sig');j=(R/'static/js/jom_governance_users_v1.js').read_text(encoding='utf-8-sig');c=(R/'static/css/jom_governance_users_v1.css').read_text(encoding='utf-8-sig')
checks={'phase owner':'JOM_GOVERNANCE_USERS_UX_PHASE1_V1' in h,'single existing API':set(re.findall(r'"(/api/[^" ]+)"',j))=={'/api/admin/users-access'},'governance measures':all(x in h for x in ['gu-review','gu-unmanaged','gu-high','gu-org-admin','gu-site-admin','gu-active']),'truth boundaries':all(x in h for x in ['Inactive users','External users','Unlicensed users','does not prove','No approved']),'privacy boundary':all(x in h for x in ['No account identifiers','email addresses','export','write actions']),'no identity exposure':all(x not in j for x in ['account_id','display_name','email','users.map']),'no writes or polling':all(x not in j for x in ['POST','PUT','PATCH','DELETE','setInterval','WebSocket','EventSource']),'contained rail':all(x in c for x in ['position:fixed','max-height:calc(100vh - 32px)','overflow-y:auto','scrollbar-gutter:stable']),'dedicated assets':'jom_governance_users_v1.js' in h and 'jom_governance_users_v1.css' in h,'single newline':all(p.read_bytes().endswith(b"\n") and not p.read_bytes().endswith(b"\n\n") for p in [R/'templates/governance_users.html',R/'static/js/jom_governance_users_v1.js',R/'static/css/jom_governance_users_v1.css'])}
f=[]
for k,x in checks.items():print(('PASS' if x else 'FAIL')+': '+k);f.append(k) if not x else None
if f:sys.exit(1)
print('VALIDATION PASS: Governance Users UX Phase 1 reuses the existing aggregate Users & Access contract without identity exposure, writes, polling or unsupported population claims.')
